spring:
  profiles:
    active: dev
  application:
    name: rest-api-workshop

app:
  jwt:
    secret: ${JWT_SECRET:tajny-klucz-tylko-do-developmentu-zmien-mnie-na-produkcji-12345}
    expiration-ms: 86400000

server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true